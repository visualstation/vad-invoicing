server.port=8080

# Database selection: h2 | mysql | postgres
app.database=h2

# Common JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# H2 configuration (active when app.database=h2)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.url=jdbc:h2:mem:logdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# MySQL configuration (activate by setting environment or JVM properties)
# app.database=mysql
# spring.datasource.url=jdbc:mysql://localhost:3306/vadinvoicing?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=secret
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# PostgreSQL configuration (activate by setting environment or JVM properties)
# app.database=postgres
# spring.datasource.url=jdbc:postgresql://localhost:5432/vadinvoicing
# spring.datasource.driverClassName=org.postgresql.Driver
# spring.datasource.username=postgres
# spring.datasource.password=secret
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Vaadin Configuration
vaadin.launch-browser=true
vaadin.whitelisted-packages=eu.ageekatyourservice.vadinvoicing

# Application name
spring.application.name=Visual Any Desk Invoicing